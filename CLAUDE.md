# CLAUDE.md — marp-theme

日本語で回答および説明してください。

## プロジェクト概要

Marp 用カスタムテーマ集。詳細は [README.md](README.md) を参照。

## プロジェクト構成

- `themes/` — テーマ CSS と Tailwind ソース
- `examples/` — サンプル Markdown と生成済み PDF
- `scripts/` — CLI・ビルドスクリプト群
- `docker/` — PDF 生成用 Docker 環境
- `docs/` — テーマ詳細・ビルドスクリプトのドキュメント

## 重要な指示

- 必要最小限の作業のみを行う
- 既存ファイルの編集を新規作成より優先
- 明示的に要求されない限り、ドキュメントファイル（*.md）や README ファイルは作成しない
- エージェントはユーザーから明示的な指示がある場合を除き、`git add` や `git commit` を実行しない
